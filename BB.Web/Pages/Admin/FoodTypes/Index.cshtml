@page
@model IndexModel

@{
    ViewData["Title"] = "Food Types";
}

<div class="border backgroundWhite container pt-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Food Type List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-page="Upsert" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp;Create New
            </a>
        </div>
    </div>

    <table id="DT_Load" class="table table-striped table-bordered" style="width:100%">
        <thead>
            <tr>
                <th>Food Type Name</th>
                <th></th>
            </tr>
        </thead>
    </table>
</div>

@section Scripts {
    <script src="~/js/foodtype.js"></script>
}
